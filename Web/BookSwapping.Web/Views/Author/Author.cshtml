@model ICollection<BookSwapping.Data.Models.Library>
@{
    ViewData["Title"] = "Author";
}

<div class="container text-center">
    <div class="text-center">Още книги от автора: <h4>@ViewData["Author"]</h4></div>

    @foreach (var b in Model)
    {

        var img = String.Format("data:image/png;base64,{0}", Convert.ToBase64String(b.Book.BookCover.ImageContent));
        <div class="card-header d-inline-block m-2">
            <img src=@img class="card-img-top" alt="@b.Book.BookCover.BookName" style="width: 150px;">
            <div class="card-body">
                <p class="card-text">
                    <a asp-action="BookDetails"
                       asp-controller="Book"
                       asp-route-id="@b.BookId" class="text-decoration-none">
                        @b.Book.BookCover.BookName
                    </a>
                </p>
            </div>
        </div>
    }
</div>

